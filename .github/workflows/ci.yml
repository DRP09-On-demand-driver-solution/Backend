name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Compile backend
        run: mvn clean compile

  test:
    runs-on: self-hosted
    needs: build
    steps:
      - name: Test backend
        run: mvn test
